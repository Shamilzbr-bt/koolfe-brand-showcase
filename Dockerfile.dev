FROM node:20-alpine

# Set working directory
WORKDIR /app

# Install dependencies for better build caching
COPY package*.json ./
RUN npm ci

# Copy source code
COPY . .

# Expose Vite dev server port
EXPOSE 8080

# Start development server
CMD ["npm", "run", "dev"]
